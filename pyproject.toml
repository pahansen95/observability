[project]
name = "observability"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []
description = "A flexible observability framework for Python applications"
readme = "README.md"
license = "MIT"

# [project.scripts]
# observability-cli = "observability.cli:main"

[dependency-groups]
build = [
    "build>=1.2.2.post1",
]
dev = [
    "mypy>=1.15.0",
    "pre-commit>=4.2.0",
    "pytest-asyncio>=1.0.0",
    "ruff>=0.11.11",
]
test = [
    "pytest>=8.3.5",
    "pytest-cov>=6.1.1",
    "pytest-mock>=3.14.1",
    "pytest-timeout>=2.4.0",
]
docs = [
    "mkdocs>=1.5.3",
]

[tool.ruff]
line-length = 120
indent-width = 2

[tool.ruff.format]
indent-style = "space"
quote-style = "double"

[tool.ruff.lint]
select = ["E", "W", "F"]
ignore = ["E501"]

[tool.ruff.lint.isort]
section-order = ["standard-library", "third-party", "local-folder"]
combine-as-imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--verbose",
    "--tb=short",
    "--strict-markers",
    "--disable-warnings",
    "--cov=src/observability",
    "--cov-report=term-missing",
    "--cov-report=html:htmlcov",
    "--cov-fail-under=85",
    "--timeout=1"
]
markers = [
    "slow: marks tests as slow",
    "integration: marks tests as integration tests",
    "unit: marks tests as unit tests"
]
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::PendingDeprecationWarning"
]
